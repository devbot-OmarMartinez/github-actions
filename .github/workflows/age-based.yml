name: Age-based Job Dispatcher

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name of the person'
        required: true
        type: string
      age:
        description: 'Age of the person'
        required: true
        type: number

jobs:
  underage:
    if: ${{ github.event.inputs.age < 18 }}
    runs-on: ubuntu-latest
    steps:
    - name: Print Name and Age
      run: echo ${{ github.event.inputs.name }} is under age ${{ github.event.inputs.age }} years old
    - name: Underage Check
      run: echo "User is underage."

  adult:
    if: ${{ github.event.inputs.age >= 18 }}
    runs-on: ubuntu-latest
    steps:
    - name: Print Name and Age
      run: echo ${{ github.event.inputs.name }} is adult ${{ github.event.inputs.age }} years old
    - name: Adult Check
      run: echo "User is an adult."